apiVersion: v1
kind: Service
metadata:
  name: bnkconfigsrv
  labels:
    app: configserver
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.services.configServer.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: configserver